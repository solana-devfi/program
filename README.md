# DevFi Solana Program

This repository contains the solana program required to run devfi.

`Proxy` is a PDA that stores the virtual owner/authority of a wallet.
`Wallet` is a PDA that is used to send and receive SOL to a user's github account. This PDA has the github user as the seed.
